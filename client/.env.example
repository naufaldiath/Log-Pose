# Client Environment Variables
# Copy this to .env and update with your values

# Development email for local testing (only used when NODE_ENV=development)
# This should match an email in your server's ALLOWLIST_EMAILS
VITE_DEV_EMAIL=dev@example.com
