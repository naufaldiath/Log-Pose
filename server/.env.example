# Log Pose 🧭 - Environment Configuration
# Your compass back home to your Mac mini development environment

# Server settings
PORT=3000
HOST=127.0.0.1
# IMPORTANT: Set to "production" when deploying
NODE_ENV=development

# Repository roots (comma-separated absolute paths)
# These are the directories containing your project repositories
REPO_ROOTS=/path/to/your/repos

# Allowlisted email addresses (comma-separated)
# Only these users can access the application
# IMPORTANT: Replace with your actual allowlisted emails
ALLOWLIST_EMAILS=user@example.com,admin@example.com

# Cloudflare Access settings
# REQUIRED in production for security
# Get these from your Cloudflare Zero Trust dashboard
CF_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com
CF_ACCESS_AUD=your-application-audience-tag

# Session limits
MAX_SESSIONS_PER_USER=3
MAX_TOTAL_SESSIONS=20
DISCONNECTED_TTL_MINUTES=20

# File limits
MAX_FILE_SIZE_BYTES=2000000

# Task runner
TASKS_ENABLED=true

# Claude CLI path (default: claude)
CLAUDE_PATH=claude

# Audit logging directory (optional)
# AUDIT_LOG_DIR=/var/log/remote-claude/audit
